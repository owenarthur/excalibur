# tests

use this for durable smoke outputs and validation artifacts that should persist outside the current run.

examples:

- spell smoke outputs
- model comparison notes
- transport verification outputs
- rendered test fixtures worth inspecting by hand

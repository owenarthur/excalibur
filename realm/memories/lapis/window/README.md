# recent memory window

put one concise daily summary here when the scaffold is instantiated.

the intended shape is:

- one file per local day
- a small rolling window
- only recent summaries here

when something ages out of the window, either distill the durable signal into
`../long-term.md`, move the rest into `../../../artifacts/` if it still
matters, or let it go.

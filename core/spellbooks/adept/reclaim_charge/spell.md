---
name: "reclaim_charge"
description: "Reclaim charge for a ritual-root run according to that ritual's hardcoded recharge amount and conditions."
---
Placeholder cast manifest for the Excalibur scaffold.

In a full implementation, each cast lives in its own folder under `core/spellbooks/<book>/<cast>/`.
This scaffold keeps only the descriptive `spell.md` layer and omits handlers and runtime code.

This cast exists to make recovery explicit. Charge should not quietly reappear. A ritual either earns recovery under its own declared conditions or it does not.

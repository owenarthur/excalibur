---
name: "codex_emanation"
description: "Spawn a coding-specialized emanation for substantial code, UI, refactor, or network-artifact work while preserving the current run's active spell surface."
---
Placeholder cast manifest for the Excalibur scaffold.

In a full implementation, each cast lives in its own folder under `core/spellbooks/<book>/<cast>/`.
This scaffold keeps only the descriptive `spell.md` layer and omits handlers and runtime code.

In a full implementation, this cast should:

- launch a coding-oriented subrun with the source run's currently open spellbooks
- allocate a caller-chosen or default amount of charge from the source run
- return code, artifacts, or structured findings for the source run to integrate
- be the obvious choice when work has real implementation weight instead of being a quick foreground reply

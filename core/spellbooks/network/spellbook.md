---
name: "network"
description: "Static network artifact scribing with stable private routes."
---
This optional spellbook governs static network artifacts and their attached threads.

Network artifacts live under `realm/artifacts/network/<slug>/`, with
published site payloads under `site/`.

Each concrete cast in this spellbook lives in its own folder beside this manifest.

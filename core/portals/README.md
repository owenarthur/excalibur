# Portals

A portal is an always-on published surface.

Portal manifests describe visible sections; mutable portal projections belong
under the responsible spirit's `runtime/<spirit>/projections/`. A portal is
a transport surface, not an authority surface. Bind locally first and publish
only through an authenticated, deliberately reviewed path.

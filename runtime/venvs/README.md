# Venvs

Isolated helper runtimes live here.

Keep renderers, acquisition tools, and other utility environments separate
from `runtime/<spirit>/`. Treat them as replaceable machinery, not durable
context or deliverables.
